[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "frejun"
version = "0.1.0"
description = "A library for integrating FreJun voice capabilities into your application"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name="FreJun", email="hello@frejun.com" }]
dependencies = ["websockets>=10.0"]